import { Bell } from "lucide-react";

export default function NotificationRibbon() {
  // const notifications = useNotificationStore((s) => s.notifications);

  // if (!notifications.length) return null;

  // return (
  //   <div className="absolute top-16 right-8 bg-white shadow-xl rounded-lg w-80 p-4 z-50">
  //     <h3 className="font-semibold mb-3">Notifications</h3>
  //     <div className="space-y-2 max-h-64 overflow-y-auto">
  //       {notifications.map((n) => (
  //         <div key={n.id} className="bg-gray-50 p-3 rounded-md">
  //           <p className="font-medium">{n.title}</p>
  //           <p className="text-sm text-gray-600">{n.message}</p>
  //           <p className="text-xs text-gray-400">{n.timestamp}</p>
  //         </div>
  //       ))}
  //     </div>
  //   </div>
  // );
   return null;
}